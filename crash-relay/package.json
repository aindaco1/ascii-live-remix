{
  "name": "ascii-vj-crash-relay",
  "version": "0.9.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "test": "node --test test/*.test.mjs",
    "check": "npm test && wrangler deploy --dry-run"
  },
  "dependencies": {
    "@octokit/auth-app": "^8.1.4"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
